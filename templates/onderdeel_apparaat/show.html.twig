{% extends 'base.html.twig' %}

{% block title %}OnderdeelApparaat{% endblock %}

{% block body %}
    <h1>OnderdeelApparaat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ onderdeel_apparaat.id }}</td>
            </tr>
            <tr>
                <th>Percentage</th>
                <td>{{ onderdeel_apparaat.Percentage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('onderdeel_apparaat_index') }}">back to list</a>

    <a href="{{ path('onderdeel_apparaat_edit', {'id': onderdeel_apparaat.id}) }}">edit</a>

    {{ include('onderdeel_apparaat/_delete_form.html.twig') }}
{% endblock %}
